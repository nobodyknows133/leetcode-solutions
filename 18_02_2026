class Solution:
    def hasAlternatingBits(self, n: int) -> bool:
        s = ""
        while n>0:
            s+=str(n%2)
            n>>=1
        x = len(s)
        a = ("01"*x)[:x]
        b = ("10"*x)[:x]
        return s==a or s==b

        
